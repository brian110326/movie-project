server:
  port: 8761

spring:
  application:
    name: service-discovery

  cloud:
    config:
      uri: http://43.200.169.229:8888
      name: movie

eureka:
  client:
    fetch-registry: false
    register-with-eureka: false

management:
  metrics:
    binders:
      system: false
      jvm: false
      processor: false
      diskspace: false