server:
  port: 8081
spring:
  application:
    name: user-service

#  h2:
#    console:
#      enabled: true
#      settings:
#        web-allow-others: true
#      path: /h2-console

  datasource:
    url: jdbc:mariadb://localhost:3306/movieDB
    driver-class-name: org.mariadb.jdbc.Driver
    username: root
    password: brian981103

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MariaDBDialect

logging:
  level:
    com.example.userservice.client: DEBUG


eureka:
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
    prefer-ip-address: true

  client:
    service-url:
      defaultZone: http://15.164.50.252:8761/eureka
    fetch-registry: true
    register-with-eureka: true


greeting:
  message: Welcome to User Service

